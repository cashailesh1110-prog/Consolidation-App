cd indas-group-consolidation
git init
git add .
git commit -m "Initial commit: Ind AS Group Consolidation app"
git branch -M main
git remote add origin https://github.com/<your-username>/<repo-name>.git
git push -u origin main
